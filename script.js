document.querySelectorAll('a[href^="#"]').forEach(a=>{a.addEventListener('click',e=>{const id=a.getAttribute('href');if(id.length>1){e.preventDefault();document.querySelector(id)?.scrollIntoView({behavior:'smooth',block:'start'});}});});function initBASliders(){document.querySelectorAll('.ba-slider').forEach(slider=>{const resize=slider.querySelector('.resize');const handle=slider.querySelector('.handle');let active=false;const start=e=>{active=true;move(e);e.preventDefault()};const end=()=>{active=false};const move=e=>{if(!active)return;const rect=slider.getBoundingClientRect();const clientX=(e.touches?e.touches[0].clientX:e.clientX);let x=clientX-rect.left;x=Math.max(0,Math.min(x,rect.width));const w=(x/rect.width)*100;resize.style.width=w+'%';handle.style.left=w+'%'};handle.addEventListener('mousedown',start);window.addEventListener('mouseup',end);window.addEventListener('mousemove',move);handle.addEventListener('touchstart',start,{passive:true});window.addEventListener('touchend',end,{passive:true});window.addEventListener('touchmove',move,{passive:false});});}window.addEventListener('load',initBASliders);